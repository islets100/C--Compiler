float PI = 3.14159; 

int gcd(int a, int b) {
    if (b == 0) return a;
    return gcd(b, a % b);
}

int main() {
    int x = 24, y = 18;
    int common = gcd(x, y); 
    
    float r = 5.0;
    float area = 0.0;
    
    if (common > 1) {
        area = PI * r * r; 
    }

    return 0;
}